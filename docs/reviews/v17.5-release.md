# v17.5 release verification

Date: 2026-09-24. Release: v17.5. Prepared from the v17.4 tree at commit `e22cd03`.

The only change is presentational: the rail footer reads "Version 17.5 · GPL-3.0 licensed", where the license text is a link to the GNU license page that opens in a new tab without an opener. The license itself (`LICENSE`, GPL-3.0) is unchanged; the file now says so on screen. No prompt bytes, dependencies, pins, storage or behavior change.

## Executed checks

- `node verify-repo.js`: **3,427 assertions passed, zero failed**; one canonical LF-only HTML; filename/release-comment agreement; all eleven frozen prompt hashes; regenerated prompt documentation; full JSX Babel transformation. The two footer version pins (`tools/anki-review-queue-tests.js`, `tools/anki-review-evidence-tests.js`) were updated to 17.5 and the queue pin now asserts the license link markup.
- `node tools/visual-browser-tests.js`: passed at 360, 768, 1024 and 1440 px, 78 checks, no application console errors.
- Playwright at 1440 px on a loopback copy: the footer text is "Version 17.5 · GPL-3.0 licensed" followed by the tagline; the link is visible (80 × 15 px), points at `https://www.gnu.org/licenses/gpl-3.0.html`, and carries `target="_blank"` with `rel="noopener noreferrer"`. At phone widths the footer stats are hidden by the existing layout rule, as before.
- All eleven pinned CDN entries re-hashed and matched; nothing changed.

## Asset identity

Filename: `Nursing-Study-Suite.v17.5.html`

SHA-256: `a7de3383e52e4d7d483811daf6ecc87606db49d9d0db973ee67feb779a7e3fb8`
